QUEUE_TYPE: RABBITMQ
QUEUE:
  KAFKA:
      HOSTS:
          - kafka.p1.gra.queue.ovh.net:9092
      TOPIC_REQUEST: queuetest.mta_request_dev
      TOPIC_ANSWER: queuetest.mta_answer_dev
      CLIENTID: clientID
      TLS: true
      SASL_USER: myuser
      SASL_PASSWORD: pass
      GROUPID: dev
  RABBITMQ:
    HOST: amqp://mta:pwd@localhost:5672/
    REQUEST_QUEUE: mta-request

CONSUMER_TYPE: INFLUX
CONSUMER:
  INFLUX:
    HOST: http://localhost:8060
    USER: user
    PASSWORD: password

SCHEDULER_TYPE: JSON
SCHEDULER:
  DB:
    driver: postgres
    datasource: postgres://mta:aliens123@27cca5e3-0ff5-4a36-9fdf-ee6abd60d2ea.pdb.ovh.net:21080/mta?sslmode=disable
  JSON:
    FILE: example/json_scheduler_checks.json